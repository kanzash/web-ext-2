import React from "react";

export const Menu = (props) => {
  return (
    <div className="menu">
      {/* Return mapped left panel options with prop for click handler */}
      {/* Right panel component that accepts active left panel menu item  */}
    </div>
  );
};

export default Menu;

//a comment
// Loop logic for left panel
// Note: be sure to return active/inactive attribute or classname on node (element in a DOM)
// active attribute in a class, get output from left panel to see which menu item is active,
//iterative method to display the multiple options in the right panel
